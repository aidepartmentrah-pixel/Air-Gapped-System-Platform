# RAH Offline Installation Platform operator UI — PL9a.
#
# Multi-stage: a real `npm run build` (the same build every dev-server
# session already exercises) produces the static bundle; nginx serves
# it and proxies /api/* to the real backend service, matching what
# vite.config.ts's dev-server proxy does for local development — the
# same relative API paths work in both environments.

FROM node:24-slim AS build

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY tsconfig*.json vite.config.ts index.html components.json ./
COPY src/ ./src/

RUN npm run build

FROM nginx:1.27-alpine

COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 8080
