# Air-Gapped System Platform — Start Here

Before doing anything else this session, read
`docs/development/SESSION_START.md` and follow it. It tells you what to
read next and how this project's documentation is organized — it's
deliberately phase-agnostic, so it stays correct regardless of what's
actually being built when you read it.
