
# Master Platform Development Matrix

|Slice|Primary Capability|Depends On|Primary Environment|Main Automated Proof|Representative Proof|Status|
|---|---|---|---|---|---|---|
|**PL0**|Runtime & Test Foundation|Architecture V1|Debian Online + Docker|startup/migration/readiness tests|Deploy base Platform|**DONE** — `platform/`, 21/21 tests pass, all required proofs verified live against the real Docker Compose stack on this Online Debian VM (see below)|
|**PL1**|Operation Framework|PL0|Debian Online|state/event/lock tests|operation polling|**DONE** — `platform/`, 21 new tests (42/42 total pass), all 11 required proofs verified live against the real running Docker Compose container (see below)|
|**PL2**|Release Discovery|PL0|Debian Online|filesystem fixture tests|scan real directory|**DONE** — `platform/`, 17 new tests (59/59 total pass), all required proofs verified live against the real running Docker Compose container using the real Golden Release fixtures (see below)|
|**PL3**|Release Import & Registry|PL0, PL2 (also PL1 in practice — see below)|Debian Online + PostgreSQL|Contract/import tests|import Golden Release|**DONE** — `platform/`, 24 new tests (76/76 total pass), all required proofs verified live against the real running Docker Compose container using 8 real Golden Release fixtures (see below)|
|**PL4**|Application State & Actions|PL3|Debian Online|decision/state tests|inspect Golden application state|**DONE** — `platform/`, 22 new tests (98/98 total pass), all required proofs verified live against the real running Docker Compose container, using a real imported Golden Release plus direct Registry seeding for installed-state scenarios (see below)|
|**PL5**|Planning & Configuration|PL3, PL4|Debian Online|input/port/plan tests|plan Golden deployment|**DONE** — `platform/`, 15 new tests (113/113 total pass), all required proofs verified live against the real running Docker Compose container, using a real imported Golden Release plus direct seeding for update/secret/backup scenarios (see below)|
|**PL6**|Fresh Installation|PL1, PL3–PL5|Debian Online + Docker|execution/failure tests|install Golden 1.0.0|**DONE** — `platform/`, 9 new tests (122/122 total pass), all required proofs verified live against the real running Docker Compose container — a real container genuinely started on the host Docker Engine (see below)|
|**PL7**|Verification & Reconciliation|PL1, PL6|Debian Online + Docker|drift/check tests|verify installed app|**DONE** — `platform/`, 21 new tests (143/143 total pass), all required proofs verified live against the real running Docker Compose container, including live drift detection after manually stopping the real container (see below)|
|**PL8**|Backup/Update/Recovery|PL1, PL3–PL7|Debian Online + Docker|transition/recovery tests|1.0.0 → 1.1.0|Tracked as two sub-slices, per the review below. **PL8a** (Backup + Update) — **DONE**, `platform/`, 14 new tests (157/157 total pass), all 7 required proofs verified live against the real running Docker Compose stack (see below). **PL8b** (Recovery) — **DONE**, `platform/`, 6 new tests (163/163 total pass), all required proofs verified live, including a real failed update genuinely unlocking `RECOVER` and a real host repaired back to `CONSISTENT` (see below)|
|**PL9**|UI & Offline Acceptance|PL2–PL8|Debian Online + Offline VM|UI/API tests|complete offline scenario|NOT STARTED — will be delivered as **PL9a** (UI Integration) then **PL9b** (Offline VM Acceptance), per the review below|

This matrix is unchanged in slice count and order from the proposal in
`1. Initial GPT Proposal.md` — the review below found the boundaries sound
overall, with two slices (`PL8`, `PL9`) recommended for tracking as paired
sub-slices for testing-signal reasons, and two clarifying notes added to
`PL3`/`PL4`. No slice was removed, reordered, merged, or redesigned.

---

# Review — Platform Slicing Proposal (pre-PL0)

Performed before any Platform code was written, mirroring the review pass
the Packager plan received before its `P0` began. Framing used, taken
verbatim from the proposal's own closing section:

> "Review these Platform slice boundaries specifically for dependency
> correctness, independent testability, feedback-signal purity, and
> implementation realism on Debian. Identify any slice that is too large,
> incorrectly ordered, circularly dependent, or impossible to test
> independently. Do not redesign the Platform or add new V1 capabilities."

Cross-checked against the frozen architecture — specifically
`docs/architecture/4.7. Stage 4 — Offline Platform Specification.md`
(Platform Public Operations §2, Internal Workflows §3, Platform State
Rules §7, Error Contract §8, and the architecture's own recommended
`§11.19 Initial Implementation Slices`) and
`docs/architecture/4.5. Stage 4 — Packager and Platform Interface
Specification.md` — not against speculation, since the review's job was to
check the proposal against what was already decided, not to invent new
opinions.

## No circular dependencies found

Walking the Master Development Matrix's `Depends On` column end to end
(`PL0 → PL1 → PL2 → PL3 → PL4 → PL5 → PL6/PL7 → PL8 → PL9`) produces a
strict DAG. Nothing in a later slice's required behavior is needed by an
earlier one.

## No incorrectly-ordered slice found

The proposal's `PL0 → PL1` ordering (Operation Framework built before any
Release-facing capability) is a deliberate departure from the
architecture's own suggested order in `§11.19` (which places Scan/Import/
Queries — Slices 2–4 — before the Operation Framework, Slice 5). This is
not a defect: `§11.19` itself only requires that the `operations` table
*migration* exist before Release Import (Slice 3) needs it — not that the
full framework (polling, locking, stale-operation detection, event API)
be built first. The proposal's version builds the full framework before
any consumer exists, at the cost of one abstract-feeling slice, in
exchange for never having to retrofit ad hoc execution tracking into
`PL2`/`PL3` later. Given the Operation Attempt Rule (architecture §7.12)
requires an operation record before *any* irreversible action — including
import — this is a legitimate, better-justified alternative to the
architecture's own suggested sequencing, not an inconsistency with it.

## One slice too large: `PL8`

`PL8 — Backup, Update and Recovery` bundles three capability groups that
the architecture treats as distinct:

- the Packager/Platform Interface Specification's own capability-group
  table (`4.5`) lists **Update** and **Backup and recovery** as two
  separate rows;
- the architecture's own recommended implementation order (`4.7 §11.19`)
  implements them as two separate slices — **Slice 9 — Update** and
  **Slice 10 — Backup and Recovery**.

`PL8`'s own "Required Tests" section already shows the cost of the
bundling: ten distinct test scenarios spanning backup success/failure,
update success/failure, configuration preservation, migration failure,
post-update verification failure, restore, recovery, and cross-application
backup rejection, all reported under one slice status. That weakens
exactly the "clean testing signal" property the proposal sets as its own
bar for slicing (see "The Key Test Dependency" in the proposal). A
regression anywhere in that surface reports as "`PL8` broken" rather than
pointing at backup, update, or recovery specifically.

**Recommendation, applied in the Master Matrix above:** track and report
`PL8` as two sub-slices rather than splitting the underlying document:

- **`PL8a` — Backup + Update.** These two cannot be fully separated —
  the Backup Requirement Rule (architecture §7.22) makes a successful,
  verified backup a hard precondition of update — so they stay paired.
- **`PL8b` — Recovery.** Recovery only activates on a controlled failure
  path (a failed update, per §7.24 Recovery History Rule) and is testable
  independently via fault injection, without needing to re-run a full
  successful update cycle for every recovery test case.

The proposal's own narrative text, objective, and required tests for
`PL8` are unchanged — this is a testing/status-tracking split, not a
redesign.

## One slice recommended for the same treatment: `PL9`

`PL9 — Operator UI and Independent Offline Proof` bundles two disciplines
that are already named separately inside the slice itself — "Part A — UI
Integration" and "Part B — Offline VM Acceptance" — but reported under one
status. UI engineering and representative hardware acceptance testing are
different failure domains (a failed offline acceptance run says nothing
about whether the UI is broken, and vice versa). For the same reason as
`PL8`, this review recommends tracking `PL9` as `PL9a` (UI Integration)
and `PL9b` (Offline VM Acceptance) in status reporting, with the
document's Part A / Part B content unchanged.

## No slice found to be impossible to test independently

Every other slice's required tests can run using only what its declared
dependencies provide:

- `PL2` (Discovery) needs only a filesystem fixture tree — no database
  writes, no Docker.
- `PL3` (Import) is often assumed to need a completed `PL2` scan first,
  since that's the real product flow. It doesn't have to: the interface
  specification (`4.5 §4.3`) explicitly allows `import_release` to accept
  a validated `candidate_directory` directly, as "an initial simpler
  implementation," instead of requiring a `candidate_id` produced by a
  real scan. `PL3`'s own tests should use that path so they don't
  accidentally couple to `PL2`'s scan output — worth stating explicitly
  in `PL3`'s implementation notes so a future session doesn't assume the
  coupling is required.
- `PL1`'s "use a synthetic test operation" instruction is exactly the
  right way to avoid the circular "build install to test the operation
  framework, build the operation framework to track install" trap.

## One clarification needed: `PL4`'s test fixtures

`PL4`'s required tests ("Installed Application," "Compatible Target,"
"Release From Different Application") all presuppose Registry state — an
existing deployment, an active Release — that in the real product is only
ever produced by `PL6`/`PL8` (Install/Update), both of which come later.
The Master Matrix correctly lists `PL4`'s only dependency as `PL3`, which
only holds if `PL4`'s tests seed that Registry state directly (test
fixtures written straight into PostgreSQL) rather than through a real
install/update operation. The proposal doesn't say this explicitly.
**Recommendation:** state directly in `PL4`'s Development Tasks that
"installed"/"active deployment" test fixtures are created via direct
Registry seeding, not through a real Platform operation — otherwise a
future implementer could reasonably (and incorrectly) conclude `PL4`
needs to wait for `PL6`.

## Two affirmative findings (no action needed, worth recording)

1. **The Platform has no open error-namespace design gap analogous to the
   Packager's deferred `PKG-*` codes.** The Packager's operational
   error-code namespace was explicitly left undesigned going into `P0`
   (see `CURRENT.md`'s Future Design Tasks). The Platform's equivalent —
   the full `PLT-*` error catalog, categories, HTTP mapping rule, and
   retryable rule — is already written into the frozen architecture
   (`4.7 §8`, categories `8.4`–`8.23`). `PL0`/`PL1` can implement the
   Platform error object directly against that catalog; no separate
   decision document is needed first, unlike the Packager's P3 staleness
   gap.
2. **`PL0`'s "Docker Failure" readiness test reuses a pattern already
   proven in this repository.** The Packager's `P0` already established
   Docker-outside-of-Docker (bind-mounting the host's
   `/var/run/docker.sock`) as the way a containerized RAH component
   reaches the host Engine on Debian, and proved the failure path by
   omitting that mount. `PL0` should reuse this directly rather than
   re-deriving it — see `packager/Dockerfile` and Packager `P0`'s
   Testing Record.

## Review conclusion

The Platform slice boundaries are sound. No slice is circularly
dependent, incorrectly ordered in a way that creates a hidden blocking
dependency, or untestable on its own declared dependencies. `PL8` and
`PL9` are recommended for paired-sub-slice status tracking due to size;
`PL3` and `PL4` get one clarifying implementation note each. None of this
redesigns the Platform or adds V1 capability — it is a review of
boundaries and sequencing only, exactly as scoped. `PL0` may begin.

---

## PL0 — Done

Built in `platform/` (Python, FastAPI + Uvicorn, SQLAlchemy + Alembic,
`docker` SDK, `psycopg`): a real Docker Compose deployment
(`platform/docker-compose.yml`) of a Platform backend container plus a
PostgreSQL 16 container, a migration mechanism, the shared API response
envelope and Platform error object (§5.2/§8.2 of the frozen architecture,
`PLT-*` codes copied verbatim from the already-written error catalog —
confirming the affirmative finding above that no error-namespace design
gap existed here), and the two required endpoints:

```
GET /api/v1/health/live
GET /api/v1/health/ready
```

Readiness inspects PostgreSQL, the host Docker Engine (via the same
Docker-outside-of-Docker bind-mount pattern already proven by the
Packager's `P0`), and the configured Release Storage path, exactly per
§6.6's example shape (`{"status": "READY", "checks": {"database": "PASS",
"docker": "PASS", "release_storage": "PASS"}}`), with a `failures` object
carrying the full structured `PlatformError` per failing check.

**All 7 required PL0 tests implemented and passing (21/21 total)** —
`platform/tests/`, run against real infrastructure, not mocks: a real
disposable PostgreSQL container (`conftest.py`'s `postgres_url` fixture)
for Registry Connectivity and the Migration Test, and the real host
Docker Engine (via the bind-mounted socket) for Docker Connectivity.
Backend Startup, Liveness, and Readiness are proven both at the unit
level (`test_health.py`, `test_app.py`) and live against the actual built
container (below).

**Every required test also re-proven live against the real Docker Compose
stack on this Online Debian VM**, not just pytest:

- **Runtime Test** — `docker compose build` + `docker compose up -d`:
  Postgres becomes `healthy`, backend runs its entrypoint's migration
  (`alembic upgrade head` — real log: `Running upgrade  -> 0001, Initial
  schema — PL0 baseline.`) and starts Uvicorn.
- **Liveness Test** — `GET /api/v1/health/live` → `200`, envelope
  `data: {"status": "UP"}`.
- **Readiness Test (healthy)** — `GET /api/v1/health/ready` → `200`,
  `{"status": "READY", "checks": {"database": "PASS", "docker": "PASS",
  "release_storage": "PASS"}}`.
- **PostgreSQL Failure** — `docker compose stop postgres`, then readiness
  → `503`, `database: FAIL`, structured `PLT-DATABASE-003` in `failures`,
  and liveness still returned `200 UP` in the same moment — the backend
  did not crash. Restarting Postgres brought readiness back to `READY`
  with no backend restart needed.
- **Docker Failure** — a disposable container built from the same image,
  run *without* the `/var/run/docker.sock` bind mount → readiness `503`,
  `docker: FAIL`, structured `PLT-DOCKER-001` (`FileNotFoundError` on the
  socket path surfaced in `details.reason`).
- **Release Storage Failure** — a disposable container with
  `RAH_RELEASE_STORAGE_PATH` pointed at a nonexistent path → readiness
  `503`, `release_storage: FAIL`, structured `PLT-STORAGE-001`.
- **Structured Result Test** — every response observed above used the
  common envelope (`success`/`data`/`warnings`/`error`/`request_id`/
  `timestamp`); every failure used the common Platform error object
  (`code`/`category`/`message`/`stage`/`retryable`/`details`/
  `request_id`/`operation_id`/`log_reference`).

**Environment note for future Platform slices on this VM**: this Online
Debian VM has no passwordless `sudo` and no pre-installed `python3-venv`
or `pip`, so `python3 -m venv` fails outright (`ensurepip is not
available`). Pip was bootstrapped user-locally via
`get-pip.py --user --break-system-packages` (no root, no system Python
modified) and dependencies installed with `pip install --user
--break-system-packages -e ".[dev]"` — this is what let the pytest suite
run directly on the host in addition to inside the container. Separately,
this VM's outbound networking resolves DNS to IPv6 first but has no IPv6
route — `curl`/`docker pull` against IPv6-preferred hosts (e.g. PyPI,
Docker Hub) hang until they time out and fall back to IPv4, so a plain
default-timeout `docker pull` can appear to fail when a longer timeout
would have succeeded. Worth remembering before concluding a future slice
is network-blocked.

**Fixture directory established**: `tests/fixtures/releases/` now exists
at the repo root (currently empty, placeholder-only) — this is where
`PL2`'s Golden Release fixtures will live, per the proposal's "Golden
Release Fixtures" section. Nothing beyond the empty directory was needed
for `PL0`, since it only had to prove the configured path is reachable,
not scan its contents.

### PL0 Testing Record

| Test ID | Test | Environment | Expected | Observed | Status |
|---|---|---|---|---|---|
| PL0-T01 | Backend Startup | Debian Online + Docker | Backend starts inside Docker | Started, migrations ran, Uvicorn serving | PASS |
| PL0-T02 | Registry Connectivity | Debian Online + Docker | Backend connects to PostgreSQL | `SELECT 1` succeeds against real Postgres 16 container | PASS |
| PL0-T03 | Migration Test | Debian Online + Docker | Empty DB migrates to expected initial schema | `alembic upgrade head` on empty DB → `platform_schema_info` row + `alembic_version = 0001` | PASS |
| PL0-T04 | Liveness Test | Debian Online + Docker | `GET /health/live` → UP | `200`, `{"status": "UP"}` | PASS |
| PL0-T05 | Readiness Test | Debian Online + Docker | Healthy deps → READY | `200`, all 3 checks `PASS` | PASS |
| PL0-T06 | Docker Failure | Debian Online + Docker | NOT READY, structured error | `503`, `docker: FAIL`, `PLT-DOCKER-001` | PASS |
| PL0-T07 | PostgreSQL Failure | Debian Online + Docker | Readiness fails, backend does not crash | `503`, `database: FAIL`, `PLT-DATABASE-003`; liveness still `200 UP` | PASS |
| PL0-T08 | Release Storage Failure | Debian Online + Docker | Missing path reported clearly | `503`, `release_storage: FAIL`, `PLT-STORAGE-001` | PASS |

## Problems Discovered

### PL0-I01 — No passwordless root, no `python3-venv`/`pip` preinstalled

See "Environment note" above. Not a Platform defect — an environment
characteristic of this specific Online Debian VM instance, worked around
without touching system Python or requiring root.

### PL0-I02 — IPv6-first DNS with no IPv6 route

See "Environment note" above. Affects any future slice that pulls a new
base image or package for the first time; a short timeout on
`docker pull`/`curl` can produce a false "no network access" conclusion.

## Decisions Made

- Framework choice (not mandated by the architecture, which only requires
  "Python HTTP API and operational engine"): FastAPI + Uvicorn +
  SQLAlchemy + Alembic + `psycopg` (v3) + the `docker` SDK — a
  conventional, well-supported stack matching the typed-request/response-
  model and OpenAPI expectations named in `4.7 §11.17`.
- PL0's initial migration (`0001`) creates one placeholder table
  (`platform_schema_info`) rather than staying completely empty, so the
  Migration Test has real DDL to assert against beyond bare `alembic`
  bookkeeping. It deliberately does **not** create `operations`,
  `applications`, or `releases` — those belong to `PL1` and `PL3`
  respectively, matching both the proposal's own task split and the
  architecture's `§11.19` note that Foundation only needs the migration
  *mechanism*, not every table, to exist yet.

## Offline Retest

Status: NOT YET REQUIRED — `PL0`'s Primary Environment is Debian Online
only; Offline VM acceptance is `PL9`'s job per the plan.

## Final Status

DONE

---

## PL1 — Done

Built `operations.py` (the Generic Operation Framework), `models.py`
(SQLAlchemy Core table definitions), and migration `0002` on top of
`PL0`'s foundation. Per the proposal's own instruction, no real deployment
operation exists yet — every test uses a synthetic `TEST_OPERATION`
`operation_type` against a randomly generated `application_id`, since
`applications` doesn't exist until `PL3`.

**Schema** (`operations`, `operation_events`, `operation_logs`):
`operations.application_id` intentionally has no foreign key yet — see
"Decisions Made" below. The per-application operation lock (§7.13) is
enforced by a **partial unique index** —
`UNIQUE (application_id) WHERE status IN ('PENDING', 'RUNNING')` — rather
than application-level locking logic. This makes lock acquisition and
release atomic and race-free for free: acquiring the lock is just "the
`INSERT` succeeded," releasing it is just "the status `UPDATE` moved the
row to a terminal state." A concurrent conflicting `INSERT` fails with a
real Postgres `IntegrityError`, caught and translated to the structured
`PLT-LOCK-001` (423).

**Built**: `create_operation` / `start_operation` / `update_stage` /
`succeed_operation` / `fail_operation` / `get_operation` /
`get_operation_events` / `get_operation_logs` / `mark_stale_operations`,
plus correlation-aware `log()` — every log line is persisted against its
`operation_id` (retrievable via `get_operation_logs`) *and* emitted
through standard Python logging with the `operation_id` bound, so it's
correlatable in container/stdout logs too. Three endpoints wired into
`app.py`: `GET /api/v1/operations/{operation_id}`, `.../events`,
`.../logs`. The per-`PlatformError`-class `http_status` attribute added
to `errors.py` (PL0's generic handler always returned `500`; PL1 needed
real per-code HTTP mapping per §8.25 — `404` for not-found, `409` for an
invalid state transition, `423` for a lock conflict).

**21 new tests added (42/42 total passing)** — `test_operations.py` (the
framework functions directly, against a real disposable PostgreSQL
container migrated to head) and `test_operations_api.py` (the same
behavior through the real HTTP surface via FastAPI's `TestClient`). All
11 required `PL1` tests covered, including two extra lock-boundary cases
beyond the minimum list (`test_lock_is_per_application`,
`test_stale_detection_frees_the_lock`) since the lock/staleness
interaction is exactly the kind of thing worth proving explicitly rather
than assuming.

**Every required test also re-proven live against the real running
Docker Compose container** (rebuilt with the `0001 → 0002` migration —
real log: `Running upgrade 0001 -> 0002, Operation Framework tables —
PL1.`), not just pytest:

- **Operation Creation / Running Transition / Successful Completion** —
  a synthetic operation driven `PENDING → RUNNING → SUCCEEDED` via
  `docker exec` directly against the live container's Python environment.
- **Polling** — `GET /api/v1/operations/{id}` on the real running
  container returned the correct final state (`SUCCEEDED`, `stage:
  EXECUTING_SCRIPT`, real timestamps, `links` to `events`/`logs`).
- **Event Ordering** — `GET .../events` returned all 4 events
  (`REQUEST_ACCEPTED`, `OPERATION_STARTED`, `STAGE_CHANGED`,
  `OPERATION_COMPLETED`) in strict sequence.
- **Error Correlation / Logs** — `GET .../logs` returned the correlated
  log lines for the same operation.
- **Lock Conflict** — a second `create_operation` for the same
  `application_id` while the first was still open raised the real
  `PLT-LOCK-001` / `423` from inside the live container.
- **Polling — 404** — `GET /api/v1/operations/00000000-...` on the real
  running container returned real HTTP `404` with structured
  `PLT-OPERATION-001` through the actual envelope.
- **Stale Operation / Lock Release** — a `RUNNING` operation was marked
  interrupted by `mark_stale_operations` inside the live container,
  ended up `FAILED` with `PLT-OPERATION-004`, and a **new** operation for
  the same `application_id` was immediately accepted afterward, proving
  the lock released as soon as the stale row left `PENDING`/`RUNNING`.

### PL1 Testing Record

| Test ID | Test | Environment | Expected | Observed | Status |
|---|---|---|---|---|---|
| PL1-T01 | Operation Creation | Debian Online + Docker | New operation is `PENDING` | `PENDING` | PASS |
| PL1-T02 | Running Transition | Debian Online + Docker | `PENDING → RUNNING` | `RUNNING`, `started_at` set | PASS |
| PL1-T03 | Successful Completion | Debian Online + Docker | `RUNNING → SUCCEEDED` | `SUCCEEDED`, `completed_at` set | PASS |
| PL1-T04 | Failed Completion | Debian Online + Docker | `RUNNING → FAILED` | `FAILED`, structured `error` stored | PASS |
| PL1-T05 | Event Ordering | Debian Online + Docker | Deterministic sequence | 4 events, sequence `1..4`, correct types | PASS |
| PL1-T06 | Polling | Debian Online + Docker | `GET /operations/{id}` returns current state | Real `200`, correct state and `links` | PASS |
| PL1-T07 | Lock Acquisition | Debian Online + Docker | First operation acquires lock | `PENDING` created | PASS |
| PL1-T08 | Lock Conflict | Debian Online + Docker | Second conflicting operation rejected | Real `PLT-LOCK-001` / `423` | PASS |
| PL1-T09 | Lock Release | Debian Online + Docker | Success/failure releases lock | New operation accepted after both success and failure | PASS |
| PL1-T10 | Stale Operation | Debian Online + Docker | Interrupted op detected, not permanently locked | `FAILED` / `PLT-OPERATION-004`, lock freed | PASS |
| PL1-T11 | Error Correlation | Debian Online + Docker | Failure carries `operation_id`/`request_id`/code/log reference | All present in structured error and `GET .../logs` | PASS |

## Problems Discovered

None. `operations` needed no foreign key to `applications` since that
table doesn't exist yet — see Decisions Made, not a defect.

## Decisions Made

- **`operations.application_id` has no foreign key yet.** `applications`
  is `PL3`'s table. The column is a plain UUID for now; the constraint
  gets added in whichever `PL3` migration creates `applications`. Tests
  use freshly generated random UUIDs as stand-in application identities,
  which is exactly what "synthetic test operation" implies.
- **The lock is a database constraint, not application logic.** Considered
  a `SELECT ... FOR UPDATE` / check-then-insert pattern first; rejected
  it because it's vulnerable to a TOCTOU race under real concurrent
  requests. The partial unique index makes the database itself the
  single source of truth for "is this application locked," which is
  simpler and provably race-free.
- **No background worker for stale-operation detection.** The
  architecture's own recommended Slice 5 mentions a "Worker" component,
  but `PL1` has no real long-running work yet for anything to interrupt.
  `mark_stale_operations()` is exposed as a plain callable; wiring it
  into a periodic check is deferred to whichever slice first has a
  process that can actually crash mid-operation — `PL6` (Fresh
  Installation), realistically.
- **`operation_logs` is a separate table from `operation_events`.**
  Matches the architecture's own split between `GET .../events`
  (structured lifecycle timeline, §5.20) and `GET .../logs` (sanitized
  diagnostic log lines, §2.8) as two distinct endpoints/concepts, rather
  than overloading one table for both purposes.

## Offline Retest

Status: NOT YET REQUIRED — `PL1`'s Primary Environment is Debian Online
only; Offline VM acceptance is `PL9`'s job per the plan.

## Final Status

DONE

---

## PL2 — Done

Built `release_discovery.py`, the `release_candidates` table (migration
`0003`), and the first four real Golden Release fixtures under
`tests/fixtures/releases/` — `valid-release-1.0.0/`,
`incomplete-release.partial/`, `missing-manifest/`, `malformed-manifest/`
— exactly the four the proposal names. Scanning performs only identity
parsing and a basic-readiness check, per the plan's own scope boundary;
full Manifest schema/Compliance Report/checksum validation stays `PL3`'s
job.

**Classification heuristic** (engineering judgment, not the full
`release-layout.yaml` structural validation, which is out of PL2's
scope): `release.yaml` present + parses + has `application.slug`/
`application.name`/`release.version` + `checksums/SHA256SUMS` present →
`READY_FOR_IMPORT`; manifest present and valid but `checksums/SHA256SUMS`
absent → `INCOMPLETE` (the Contract itself generates `SHA256SUMS` last
during finalization, so its absence is a reasonable "transfer isn't
finished" signal); `release.yaml` missing but other Contract-reserved
structure present → `INVALID_MANIFEST`; `release.yaml` missing with
nothing else recognizable present → `INCOMPLETE`; `release.yaml` present
but fails to parse or is missing required identity fields →
`INVALID_MANIFEST`. `ALREADY_IMPORTED`/`PREVIOUSLY_REJECTED`/
`IDENTITY_CONFLICT` are part of the full architecture-level enum but
aren't reachable from PL2 alone (no Release Registry exists until `PL3`)
— confirmed the plan's own required fixtures/tests don't exercise them
for exactly that reason.

**Built**: `scan_releases` / `list_candidates` / `get_candidate` in
`release_discovery.py`; `release_candidates` upserted by `directory_name`
so a repeat scan of the same physical directory updates the existing row
instead of creating a duplicate (mirrors PL1's "the database is the
source of truth" approach, just without a partial-unique-index lock
since there's no concurrency concern here). Three endpoints wired into
`app.py`: `POST /api/v1/release-candidates/scan`,
`GET /api/v1/release-candidates`, `GET /api/v1/release-candidates/{id}`.
`ScanReleasesRequest` matches architecture §4.2 exactly (`rescan_known_releases`,
`include_rejected_candidates`) with **no path field** and
Pydantic `extra="forbid"` — the mechanism behind the "Arbitrary Directory"
test. New error `CandidateNotFoundError` reuses `PLT-STORAGE-004`
("Release Package missing") since the catalog has no literal "candidate
not found by id" code — flagged in `errors.py` for the same later
namespace-review discussion as the Packager's `PKG-RUNTIME-*` outliers.

**Also fixed while building this slice, not scoped to PL2 specifically**:
FastAPI/Pydantic's `RequestValidationError` was bypassing the common
response envelope entirely (returning FastAPI's own unrelated
`{"detail": [...]}` shape) since PL0 — nothing had triggered a request
validation failure until PL2's `extra="forbid"` check. Added a proper
handler (`RequestValidationFailedError`, `PLT-INPUT-003`, `422`) so this
class of failure is consistent with every other error response, live-
verified before and after the fix.

**17 new tests added (59/59 total passing)** — `test_release_discovery.py`
(the module directly, against real disposable-directory copies of the
real fixtures and a real disposable PostgreSQL container) and
`test_release_discovery_api.py` (the same behavior through the real HTTP
surface). One real test-isolation bug found and fixed along the way:
`conftest.py`'s `db_engine` fixture truncated `operations`/
`operation_events`/`operation_logs` between tests but not the new
`release_candidates` table, so later tests saw candidates left behind by
earlier ones — fixed by adding it to the teardown.

**Every required test also re-proven live against the real running
Docker Compose container**, using the real fixture directory already
bind-mounted at `/data/releases` since `PL0` (`docker-compose.yml`'s
`../tests/fixtures/releases:/data/releases` — no change needed there):

- **Valid Candidate / Incomplete Copy / Missing Manifest** — a real
  `POST /release-candidates/scan` against all 4 real fixture directories
  simultaneously returned exactly the expected classification for each:
  `valid-release-1.0.0` → `READY_FOR_IMPORT`,
  `incomplete-release.partial` → `INCOMPLETE` (with the real "checksums
  missing" issue message), `missing-manifest` and `malformed-manifest` →
  `INVALID_MANIFEST` (the real PyYAML `ScannerError` text surfaced in
  `issues` for the malformed case).
- **Repeat Scan** — scanning twice in a row against the live container
  returned identical `candidate_id` values for all 4 candidates both
  times; `GET /release-candidates` confirmed exactly 4 rows, not 8.
- **Arbitrary Directory** — `POST /release-candidates/scan` with
  `{"path": "/etc"}` on the live container returned real `422` /
  `PLT-INPUT-003` through the common envelope, both before and after the
  validation-handler fix (confirming the fix didn't change the rejection,
  only its shape).
- **Read-Only Behavior** — `SELECT COUNT(*) FROM operations` on the live
  container returned `0` immediately after a real scan — scanning never
  touches the Operation Framework at all, consistent with §7.12 (scan
  isn't in the Operation Attempt Rule's list).

### PL2 Testing Record

| Test ID | Test | Environment | Expected | Observed | Status |
|---|---|---|---|---|---|
| PL2-T01 | Empty Storage | Debian Online + Docker | Zero candidates | `candidate_count: 0` | PASS |
| PL2-T02 | Valid Candidate | Debian Online + Docker | Detected correctly | `READY_FOR_IMPORT`, correct slug/version | PASS |
| PL2-T03 | Multiple Candidates | Debian Online + Docker | All returned predictably | 4 candidates, stable sorted order | PASS |
| PL2-T04 | Incomplete Copy | Debian Online + Docker | Classified `INCOMPLETE` | `INCOMPLETE`, real issue message | PASS |
| PL2-T05 | Missing Manifest | Debian Online + Docker | Classified invalid | `INVALID_MANIFEST` | PASS |
| PL2-T06 | Repeat Scan | Debian Online + Docker | No uncontrolled duplicates | Same `candidate_id`s, 4 rows not 8 | PASS |
| PL2-T07 | Arbitrary Directory | Debian Online + Docker | Ordinary caller cannot scan an arbitrary path | Real `422` / `PLT-INPUT-003` | PASS |
| PL2-T08 | Read-Only Behavior | Debian Online + Docker | No Registry state change | `operations` row count `0` after scan | PASS |

## Problems Discovered

### PL2-I01 — `RequestValidationError` bypassed the common envelope

Present since `PL0` but never triggered until `PL2`'s `extra="forbid"`
check was the first thing to actually fail request validation. Fixed —
see "Also fixed" above. Worth remembering for any future slice that adds
its own request model: this class of failure is now handled once, at the
app level, not per-endpoint.

### PL2-I02 — Test-isolation gap in `conftest.py`

`db_engine`'s teardown didn't truncate the new `release_candidates`
table. Fixed by adding it to the existing teardown list.

## Decisions Made

- **Classification heuristic is PL2-local engineering judgment, not the
  Contract's full structural validation.** Explicitly scoped down per the
  plan's own words ("only the amount of parsing required to determine
  candidate identity and basic readiness"). `PL3`'s `import_release` will
  do the real Manifest schema / Compliance Report / checksum validation
  against `contracts/1.0/`.
- **`checksums/SHA256SUMS` presence is the `INCOMPLETE` vs.
  `READY_FOR_IMPORT` signal** for an otherwise-valid manifest, because the
  Release Contract itself generates that file last during finalization
  (`release-layout.yaml`'s own checksums notes). An approximation, not a
  guarantee — acceptable because `PL3`'s real checksum verification is
  the actual gate before anything is trusted.
- **`CandidateNotFoundError` reuses `PLT-STORAGE-004`** rather than
  inventing a new code outside the frozen `§8.8` catalog. Flagged
  alongside the Packager's existing namespace outliers for a later
  dedicated review, not treated as settled.
- **The Golden Release fixtures' `release.yaml` files are identity-only**,
  not full-schema-valid manifests (missing `source`, `compatibility`,
  `deployment`, `docker`, etc. — all required by
  `contracts/1.0/release-manifest.schema.json`). Documented directly in
  the fixture file's own header comment so `PL3` knows to expand
  `valid-release-1.0.0/release.yaml` into a truly complete manifest
  rather than assuming it already is one.

## Offline Retest

Status: NOT YET REQUIRED — `PL2`'s Primary Environment is Debian Online
only; Offline VM acceptance is `PL9`'s job per the plan.

## Final Status

DONE

---

## PL3 — Done

Built `release_import.py`, the real Registry tables
(`applications`/`releases`/`release_storage`, migration `0004`), and 8
real Golden Release fixtures under `tests/fixtures/releases/` — 6 the
plan names (`valid-release-1.0.0` rebuilt to be fully Contract-valid,
`valid-release-1.1.0`, `invalid-checksum`, `failed-compliance`,
`unsupported-contract`, `identity-conflict`) plus 2 extra
(`wrong-architecture`, `platform-version-too-old`) to give the
architecture/Platform-compatibility checks real coverage beyond the
minimum. Every fixture's manifest was verified to genuinely validate (or
fail, where intended) against the real, frozen
`contracts/1.0/release-manifest.schema.json` and
`compliance-report.schema.json` before being used in any test — not
hand-typed and assumed correct.

**Scope boundary, confirmed against the architecture before writing any
code**: the Platform's own `import_release` re-validates a narrower set
than the Packager's full `RC-*` Release Validator catalog
(`contracts/1.0/validation-rules.json`, 11 categories, ~50 rules). Per
§3.5 steps 6–7 ("locate and read the Compliance Report; require a
successful compliance result"), the Platform trusts the Compliance
Report's `overall_result` for everything it already certifies (directory
structure, script existence, configuration/database/security rules,
etc.) rather than re-running the Packager's Validator pipeline. What the
Platform re-validates directly: Manifest schema (real `jsonschema.validate`
against the mounted, real schema file — no embedded copy, see Decisions
Made), Release Contract version, the Compliance Report's own
`overall_result`, the checksum set (real `sha256` recomputation, not
approximated), the Release fingerprint, architecture/Platform
compatibility, and identity conflict.

**Real dependency gap found and closed**: the Master Matrix (from the
original, un-redesigned proposal) lists `PL3`'s dependency as `PL0, PL2`
only. In practice `PL3` also depends on `PL1` — architecture §7.12's
Operation Attempt Rule explicitly names `import` as requiring an
Operation record before irreversible work, and `import_release` does use
the Generic Operation Framework for real (see below). This isn't a defect
in the accepted review (`PL1` already precedes `PL2`/`PL3` in the slice
order, so nothing was ever blocked), just an omission in the dependency
column worth recording rather than silently fixing.

**Operation tracking boundary** (a real design decision, not obvious from
the plan): failures that happen before `release.yaml` can be parsed and
trusted (missing/unreadable manifest, unsupported manifest schema
version, unsupported Contract version, structural schema failure) are
**not** tracked as Operations — there's no reliable `application_id` yet
to attribute them to. From the point the manifest is trusted onward
(Compliance Report, checksums, fingerprint, compatibility, identity
conflict, the actual registry write), every attempt is tracked through
`PL1`'s framework, live-verified below: 1 `SUCCEEDED` + 3 `FAILED`
operations for 4 real import attempts that reached that point, and
correctly **zero** operations for the one that didn't
(`unsupported-contract`).

**Built**: `_load_and_validate_manifest` / `_check_compliance_report` /
`_verify_checksums` / architecture+Platform compatibility checks /
`_find_or_create_application` / `_write_release_and_storage`, all
orchestrated by `import_release()`. `POST
/api/v1/release-candidates/{candidate_id}/import` wired into `app.py`.
`contracts/1.0/` mounted read-only into the Compose container
(`RAH_CONTRACTS_PATH=/contracts/1.0`) rather than embedded as a Python
copy — see Decisions Made.

**PL2 amendment, not a redesign**: `release_discovery.scan_releases` now
cross-checks structurally-ready candidates against the real Registry
(`_check_registry_state`), correctly reporting `ALREADY_IMPORTED` and
`IDENTITY_CONFLICT` — states the PL2 write-up explicitly said "aren't
reachable from PL2 alone... require comparing against the Release
Registry, which doesn't exist until PL3." This closes that exact,
already-flagged gap now that the Registry exists, without reopening any
of PL2's own reviewed boundaries. Also fixed a real bug this surfaced:
`scan_releases` previously hardcoded `already_imported: False` on every
scan, which would have silently erased the "already imported" marking on
a re-scan after import — fixed before it ever shipped.

**PL1 amendment, not a redesign**: adding the deferred foreign key from
`operations.application_id` → `applications.application_id` (which PL1's
own write-up said would happen "once `applications` exists") meant PL1's
own tests, which used bare random UUIDs as synthetic application
identities, needed to seed a real `applications` row first. Fixed via a
new `seed_application()` test helper in `conftest.py`; all `PL1` tests
still pass unchanged in behavior, just against a real (not merely
plausible) application identity now.

**24 new tests added (76/76 total passing)** — `test_release_import.py`
(all 9 required scenarios plus 2 extra compatibility tests, against real
disposable-directory copies of the real fixtures and a real PostgreSQL
container) and `test_release_import_api.py` (the same behavior through
the real HTTP surface, plus the PL2-amendment re-scan test). One real bug
found and fixed while writing these tests:
`import_release`'s failure handler recorded the correctly-wrapped
`PartialImportPreventedError` into the Operation record but then
re-raised the original raw exception to the caller via a bare `raise` —
callers would have seen an unstructured `RuntimeError` instead of the
structured error actually written to history. Fixed by re-raising the
wrapped error (`raise error from exc`) unless it was already a
`PlatformError`.

**Every required test also re-proven live against the real running
Docker Compose container**, not just pytest:

- **First Release of New Application** — real `POST .../import` on
  `valid-release-1.0.0` returned a real `application.id`/`release_id`,
  `storage.state: AVAILABLE`.
- **Same Version / Same Fingerprint** — real re-scan after import
  reported `discovery_state: ALREADY_IMPORTED`, `already_imported: true`
  — the PL2-amendment proof, live.
- **Invalid Checksum / Failed Compliance Report / Identity Conflict** —
  all three real `POST .../import` calls on the live container returned
  the correct structured error (`PLT-INTEGRITY-002` / `PLT-IMPORT-004` /
  `PLT-IMPORT-005`) and correct HTTP status (`422` / `422` / `409`).
- **Unsupported Contract** — real `422` / `PLT-CONTRACT-002`.
- **Operation tracking boundary** — queried the live container's
  `operations` table directly after the above attempts: exactly 1
  `SUCCEEDED` + 3 `FAILED` rows (checksum, compliance, identity-conflict
  failures) and correctly **zero** rows attributable to the
  unsupported-contract attempt, confirming the boundary decision above
  holds in the real running system, not just in unit tests.
- **No partial/incorrect state** — after all of the above, the live
  container's `applications`/`releases` tables contained exactly 1 row
  each (only the one real success), confirming the reject paths left
  nothing behind.

### PL3 Testing Record

| Test ID | Test | Environment | Expected | Observed | Status |
|---|---|---|---|---|---|
| PL3-T01 | First Release of New Application | Debian Online + Docker | New Application + Release created | Real `application_id`/`release_id`, `AVAILABLE` | PASS |
| PL3-T02 | Second Release of Same Application | Debian Online + Docker | Same Application, new Release | Same `application.id`, versions `1.0.0`/`1.1.0` | PASS |
| PL3-T03 | Invalid Checksum | Debian Online + Docker | Import rejected | Real `422` / `PLT-INTEGRITY-002` | PASS |
| PL3-T04 | Failed Compliance Report | Debian Online + Docker | Import rejected | Real `422` / `PLT-IMPORT-004` | PASS |
| PL3-T05 | Unsupported Contract | Debian Online + Docker | Import rejected | Real `422` / `PLT-CONTRACT-002`, no Operation created | PASS |
| PL3-T06 | Same Version / Same Fingerprint | Debian Online + Docker | Predictable no-op / already-imported | `already_imported: true`, same `release_id`, no duplicate row | PASS |
| PL3-T07 | Same Version / Different Fingerprint | Debian Online + Docker | Reject with identity conflict | Real `409` / `PLT-IMPORT-005` | PASS |
| PL3-T08 | Partial Registry Failure | Debian Online + Docker | No partially valid import remains | Injected fault → 0 rows in `releases`/`release_storage`, Operation `FAILED` | PASS |
| PL3-T09 | Immutability | Debian Online + Docker | Import does not modify Release Package | Byte-identical file hashes before/after | PASS |

## Problems Discovered

### PL3-I01 — Failure handler re-raised the wrong exception

See "24 new tests" above. Found by the Partial Registry Failure test
itself, fixed before any external behavior shipped.

### PL3-I02 — `scan_releases` would have erased `already_imported` on re-scan

See "PL2 amendment" above. Found while implementing the Registry
cross-check, fixed in the same change.

### PL3-I03 — `platform/` directory name risks shadowing stdlib `platform`

Discovered while writing the architecture-compatibility check: this
repo's own top-level `platform/` directory could shadow Python's stdlib
`platform` module for local (non-container) pytest runs depending on
`sys.path` ordering. Avoided entirely by using `os.uname().machine`
instead of `platform.machine()` — no import of the stdlib module at all.
Worth remembering for any future slice tempted to `import platform`.

## Decisions Made

- **`contracts/1.0/*.json` are mounted read-only into the container
  (`RAH_CONTRACTS_PATH`), not embedded as Python copies.** The Packager's
  own precedent (`P6`) embeds a verbatim Python-dict copy of the
  manifest schema plus a dedicated drift test against the real file.
  Mounting the real file directly is strictly better here — zero drift
  risk by construction, no duplicate to keep in sync, and it costs
  nothing extra since the Platform already mounts Release Storage the
  same way. `Config.contracts_path` defaults to `/contracts/1.0` (the
  Compose mount path) with an env var override for tests.
- **Release fingerprint = `sha256` of `release.yaml`'s raw bytes.**
  Architecture §7.3 requires a fingerprint but never specifies an exact
  algorithm (a COMPLETED-style gap, like several others in this
  Contract). Cheap, deterministic, and sufficient for §7.3's actual
  purpose — detecting whether two candidates claiming the same
  application+version declare the same content. Defined once in
  `release_discovery.py` (`compute_fingerprint`) and reused by
  `release_import.py`, not duplicated.
- **`_find_or_create_application` is select-then-insert, not
  concurrency-hardened against a true race.** `applications.slug` is
  `UNIQUE` at the database level, so a genuine race would surface as a
  real `IntegrityError` rather than silently corrupting anything, but
  the code doesn't currently catch and retry that case. Matches §7.13's
  own allowance ("initial Platform may serialize... if that
  substantially simplifies Version 1") — not exercised by any required
  test, flagged rather than silently assumed away.
- **`_check_shared_services` has no real service inventory to check
  against**, so any manifest that declares a *required* shared service is
  honestly rejected (`PLT-COMPATIBILITY-004`) rather than silently
  accepted. None of the Golden Fixtures declare one. A real check is
  Period B/infrastructure-inventory territory, not Period A Platform.
- **`CandidateNotFoundError`'s `PLT-STORAGE-004` reuse (from `PL2`)
  turned out to double as the "unknown candidate_id passed to import"
  case too** — confirmed live (`404` on an unknown `candidate_id` at the
  import endpoint), no new code needed.

## Offline Retest

Status: NOT YET REQUIRED — `PL3`'s Primary Environment is Debian Online +
PostgreSQL only; Offline VM acceptance is `PL9`'s job per the plan.

## Final Status

DONE

---

## PL4 — Done

Built `application_query.py` — the read-only "what exists, what's
active, what may the operator do" layer — plus a minimal `deployments`
table and `applications.active_deployment_id` (migration `0005`, the
schema `get_active_deployment`/`get_available_actions` read from). No
lifecycle script runs anywhere in this slice, exactly per its own
Objective.

**The exact gap the pre-PL0 review flagged for this slice, confirmed and
handled as anticipated**: PL4's required tests ("Installed Application,"
"Compatible Target," etc.) presuppose an active deployment, but nothing
writes one for real until `PL6`. Per the review's own recommendation,
`PL4`'s tests seed `deployments` directly (`seed_active_deployment` in
`conftest.py`) rather than waiting on `PL6` — the Master Matrix's `PL4`
dependency stays correctly `PL3`-only, confirmed by construction: nothing
in `application_query.py` calls into `release_import.py` or `operations.py`
to produce test state.

**Six action types get real, evidence-based logic**
(`INSTALL`/`UPDATE`/`DOWNGRADE`/`REINSTALL`/`VERIFY`/`BACKUP`), each
checking actual Registry state — active deployment presence, target
Release's `manifest.deployment.supported_operations`, storage state,
`transition.accepted_installed_versions` — never a stub. **`RECOVER`
always returns unsupported** with real reasoning (`PLT-RECOVERY-001`, "no
failed operation to recover from") since no failure/recovery tracking
exists until `PL8` — exactly the plan's own "some may always return
unsupported... the important part is correct reasoning" allowance, not a
placeholder.

**`operational_health` reports `UNKNOWN` for any installed application,
never a fabricated `HEALTHY`.** The Platform has no real host
verification evidence until `PL7` — reporting anything more confident
than `UNKNOWN` would violate §5.24's Result Authority Principle ("shall
not claim more than the evidence supports"), live-confirmed below.

**`deployment_state` mapping** (`ACTIVE`/`PREVIOUSLY_DEPLOYED`/
`NEVER_DEPLOYED`/`REMOVED_FROM_STORAGE`) is a COMPLETED-style
interpretation: the architecture's enum also lists a plain `AVAILABLE`
value whose distinction from `NEVER_DEPLOYED` isn't spelled out further
in what's been read so far, so this slice never emits it — flagged in
Decisions Made rather than guessed silently.

**22 new tests added (98/98 total passing)** — `test_application_query.py`
(all 8 required scenarios plus extras for list/get endpoints, active
deployment, and the VERIFY/BACKUP/RECOVER logic) and
`test_application_query_api.py` (the same behavior through the real HTTP
surface). A new `seed_release()` test helper inserts a `releases` row
directly with a hand-built manifest, bypassing `release_import.py`
entirely — appropriate here since PL4's decision logic only cares about
Registry state, not how a Release arrived, and it lets each test exercise
an exact `supported_operations`/`transition` combination without a
dedicated Golden Fixture directory per scenario (unlike `PL2`/`PL3`,
which need real fixture directories since they test filesystem/Contract
validation itself).

**Every required test also re-proven live against the real running
Docker Compose container**, combining a real imported Golden Release
(via the real `scan` → `import` flow) with direct Registry seeding for
the installed-state half — not a synthetic-only proof:

- **Fresh Application** — real `GET .../actions` on the just-imported,
  not-yet-installed `golden-test-app` returned `INSTALL: true`, every
  other action blocked with `PLT-TRANSITION-007`/`PLT-APPLICATION-003`.
- **Installed Application** — after directly seeding a real `deployments`
  row (simulating what `PL6` will do for real) and re-querying the same
  live container: `INSTALL` now `false`/`PLT-APPLICATION-002`, `VERIFY`
  and `BACKUP` now `true`, `operational_health: UNKNOWN` (not `HEALTHY`).
- **Release Package Missing** — a second real application seeded with a
  Release marked `REMOVED_FROM_STORAGE`: `GET /releases/{id}` still
  returned it in full (historical visibility intact), while
  `GET .../actions` blocked `INSTALL` specifically with
  `PLT-RELEASE-002` — the precise distinction the required test asks for.
- **`GET /applications/{id}/active-deployment`** — returned the real
  seeded deployment's `release_id`/`version`/`verification_status`
  through the live HTTP surface.

### PL4 Testing Record

| Test ID | Test | Environment | Expected | Observed | Status |
|---|---|---|---|---|---|
| PL4-T01 | Fresh Application | Debian Online + Docker | Install allowed, update not | `INSTALL: true`, `UPDATE: false` | PASS |
| PL4-T02 | Installed Application | Debian Online + Docker | Fresh install blocked | `INSTALL: false` / `PLT-APPLICATION-002` | PASS |
| PL4-T03 | Compatible Target | Debian Online + Docker | Update allowed | `UPDATE: true` | PASS |
| PL4-T04 | Unsupported Transition | Debian Online + Docker | Update rejected, reason given | `UPDATE: false` / `PLT-TRANSITION-003` | PASS |
| PL4-T05 | Same Version | Debian Online + Docker | No update | `UPDATE: false` / `PLT-TRANSITION-006` | PASS |
| PL4-T06 | Release From Different Application | Debian Online + Docker | Rejected | Real `422` / `PLT-RELEASE-003` | PASS |
| PL4-T07 | Downgrade Unsupported | Debian Online + Docker | Downgrade unavailable | `DOWNGRADE: false` / `PLT-TRANSITION-004` | PASS |
| PL4-T08 | Release Package Missing | Debian Online + Docker | Historical Release visible, action blocked | Release visible, `INSTALL: false` / `PLT-RELEASE-002` | PASS |

## Problems Discovered

None. The one open interpretive question (`deployment_state`'s unused
`AVAILABLE` enum value) is recorded in Decisions Made, not treated as a
defect.

## Decisions Made

- **`deployments`/`applications.active_deployment_id` built now, written
  to only by test seeding.** The alternative — deferring any
  `deployments`-shaped schema until `PL6` — would have left `PL4` with
  nowhere to read "is this installed" from at all. Building the schema
  now and writing to it only via direct seeding (never through
  `application_query.py` itself, which stays strictly read-only) keeps
  the slice's own "no lifecycle scripts execute" boundary intact while
  still answering its stated question for real.
- **`operational_health: UNKNOWN` for every installed application, never
  `HEALTHY`.** Considered defaulting to `HEALTHY` when a `verification_status`
  of `PASS` is present on the seeded deployment; rejected because `PL4`
  itself never performed or witnessed that verification — trusting a
  seeded field would be reporting evidence the Platform doesn't actually
  have yet. `PL7` (Verification & Reconciliation) is what earns the right
  to report real health.
- **`deployment_state`'s `AVAILABLE` value is never emitted.** The
  architecture's enum lists `AVAILABLE`, `ACTIVE`, `PREVIOUSLY_DEPLOYED`,
  `NEVER_DEPLOYED`, `REMOVED_FROM_STORAGE` without fully disambiguating
  `AVAILABLE` from `NEVER_DEPLOYED` for an imported-but-uninstalled
  Release in what's been read of the architecture so far. This slice
  treats them as the same case and always emits `NEVER_DEPLOYED`. Worth a
  closer architecture re-read if a future slice needs the distinction;
  not blocking anything today.
- **`seed_release()` bypasses `release_import.py` entirely for test
  setup.** Deliberate: `PL4` tests need precise, varied
  `supported_operations`/`transition` combinations that would require a
  new Golden Fixture directory per scenario if built through the real
  import path. Since `PL4`'s own logic only reads Registry rows, not
  Release Package files, seeding the row directly is honest test setup,
  not a shortcut around anything `PL4` itself is responsible for
  validating.

## Offline Retest

Status: NOT YET REQUIRED — `PL4`'s Primary Environment is Debian Online
only; Offline VM acceptance is `PL9`'s job per the plan.

## Final Status

DONE

---

## PL5 — Done

**Amended during `PL6`**: `validate_deployment_inputs` originally also
did a live port-availability check beyond its own required tests. `PL6`
found this duplicated architecture's own dedicated "recheck ports
immediately before execution" step and produced the wrong error code for
`PL6`'s Port Conflict test — removed, keeping only the datatype check
this slice's own required tests need. See `PL6`'s write-up ("Real design
overlap found and fixed") for the full reasoning.

Built `deployment_planning.py` — `prepare_installation`,
`prepare_update`, `validate_deployment_inputs`, `suggest_available_ports`
— plus `deployment_configuration` (migration `0006`), the schema
`prepare_update`'s "preserve existing configuration" (§7.15) reads from.
Nothing in this module writes to `deployments`, `applications`, or
Docker — confirmed by construction (no `INSERT`/`UPDATE` statement
anywhere in the module targets those tables) and live-verified below.

**Real, live port checks, not approximated.** `suggest_available_ports`
and the port-datatype branch of `validate_deployment_inputs` both call a
real `socket.bind()` against this host — the same "provisional, not a
reservation" property required by §7.17 is demonstrated for real: a test
suggests a port, something else binds it, and a repeat check correctly
reports it unavailable, live-verified against the real container too.

**Reused `PL4`'s transition logic rather than re-implementing it.**
`prepare_update` calls `application_query.get_available_actions()`
internally and reads the `UPDATE` action's `blocking_reasons` directly,
rather than duplicating the accepted-source-version/unsupported-update
checks a second time. The two must always agree on whether an update is
valid — reusing the already-tested function guarantees it by
construction instead of by discipline.

**Refactoring done along the way, not scope creep**: `application_query.py`'s
three internal lookup helpers (`get_application_row`, `get_release_row`,
`get_storage_state`) were renamed from `_`-prefixed private functions to
proper shared internal functions, since `deployment_planning.py`
genuinely needed them and importing another module's "private" functions
across a package boundary was a real smell worth fixing before it became
an established pattern. No behavior changed; verified via the full
existing suite before writing any `PL5` code.

**Configuration input classification is real**, not just structural
pass-through: `fixed` sources surface the manifest's declared default;
`operator`/`generated`/`platform` sources report `current_value: null`
for a fresh install (nothing chosen yet); a `preserved` source (update
only) pulls the real stored value from `deployment_configuration`.
Secret-flagged inputs **never** get a `current_value` key at all, fresh
or preserved — only `value_state` (`REQUIRED`/`WILL_GENERATE`/
`PRESERVED`) per §7.16's storage-shape guidance, live-verified with a
full plan JSON dump containing no secret value anywhere.

**15 new tests added (113/113 total passing)** —
`test_deployment_planning.py` (all 10 required scenarios) and
`test_deployment_planning_api.py` (the same behavior through the real
HTTP surface). `conftest.py`'s `seed_release` helper (built for `PL4`)
was extended with optional `configuration_inputs`/`docker_images`/
`database`/`verification_checks` parameters — backward compatible with
every existing `PL4` test — plus a new `seed_deployment_configuration`
helper, following the same "seed directly, no real install exists until
`PL6`" pattern already established.

**Every required test also re-proven live against the real running
Docker Compose container**, combining a real imported Golden Release
(full scan → import flow) with direct Registry seeding for the
update/backup/secret scenarios:

- **Installation Plan** — real `POST .../installation-plan` on the just-
  imported `golden-test-app` returned the real canonical path, compose
  project name, `expected_services: ["backend"]`, the real `APP_PORT`
  input declaration, and baseline verification checks.
- **Missing Required Input / Invalid Datatype / Unknown Config Key** —
  three real `POST .../validate-inputs` calls against the live container
  returned `PLT-INPUT-002`/`PLT-INPUT-003`/`PLT-INPUT-004` respectively;
  a fourth call with a fully valid payload returned `valid: true`.
- **Port Suggestion** — real `POST /host/ports/suggestions` with
  `preferred_ports: [8501]` (free on the live container) returned it
  first, plus two real alternatives.
- **Update Plan / Mandatory Backup / Secret Preservation** — a real
  active deployment and a second Release (`database.backup_before_update.required:
  true`) were seeded directly in the live container; the resulting real
  `POST .../update-plan` response showed `APP_PORT` preserved at its
  real seeded value (`9001`, `source: "preserved"`) and
  `backup: {"required": true, "supported": true}` appearing explicitly.
- **Planning Safety** — queried the live container directly after all of
  the above: `deployments` count unchanged at `1`, and the application's
  active deployment still pointed at the original `1.0.0` release, not
  the planned target.

### PL5 Testing Record

| Test ID | Test | Environment | Expected | Observed | Status |
|---|---|---|---|---|---|
| PL5-T01 | Installation Plan | Debian Online + Docker | Path/config/ports/services/checks all correct | All fields matched real manifest | PASS |
| PL5-T02 | Port Suggestion | Debian Online + Docker | Occupied preferred port → alternative suggested | Real socket check, alternative returned | PASS |
| PL5-T03 | Port Race Revalidation | Debian Online + Docker | Suggestion stays provisional | Re-check after real occupation reports unavailable | PASS |
| PL5-T04 | Missing Required Input | Debian Online + Docker | Validation fails | Real `PLT-INPUT-002` | PASS |
| PL5-T05 | Invalid Datatype | Debian Online + Docker | Validation fails | Real `PLT-INPUT-003` | PASS |
| PL5-T06 | Unknown Config Key | Debian Online + Docker | Rejected | Real `PLT-INPUT-004` | PASS |
| PL5-T07 | Secret Value | Debian Online + Docker | Not echoed through response/log | No `current_value`/plaintext secret anywhere in plan JSON | PASS |
| PL5-T08 | Update Plan | Debian Online + Docker | Preserves existing configuration | `APP_PORT` preserved at real seeded value | PASS |
| PL5-T09 | Mandatory Backup | Debian Online + Docker | Appears explicitly in update plan | `backup.required: true` | PASS |
| PL5-T10 | Planning Safety | Debian Online + Docker | No Docker/active-deployment changes | `deployments` count unchanged, active release unchanged | PASS |

## Problems Discovered

None. The `application_query.py` private-helper rename was a proactive
cleanup, not a bug fix.

## Decisions Made

- **`application_query.py`'s lookup helpers made shared/public rather
  than duplicated.** The alternative — writing `deployment_planning.py`'s
  own copies of "look up an application/release row" — would have
  created two implementations of the same lookup that could silently
  drift (e.g. a future error-message change applied to only one copy).
  Renamed instead of duplicated, no behavior change, full suite green
  before and after.
- **`prepare_update` calls `application_query.get_available_actions()`
  directly for transition validity** rather than re-deriving
  accepted-source-version/unsupported-update logic. Same reasoning as
  above, applied at the function level instead of the helper level.
- **Port checks are real `socket.bind()` calls, not simulated.** Matches
  this project's established discipline (real Docker, real Postgres,
  real checksums throughout `PL0`-`PL4`) — a fake/mocked port-availability
  check would not actually prove §7.17's "provisional, not a
  reservation" property, which is the entire point of the required Port
  Race Revalidation test.
- **`deployment_configuration.value` stays `NULL` for every
  `secret=true` row**, with only a `secret_reference` stand-in stored —
  no real secret ever touches the database in plaintext, consistent with
  §7.16 and with how the Packager already treats real secrets in its own
  test suite (mocked/never-persisted-plaintext).

## Offline Retest

Status: NOT YET REQUIRED — `PL5`'s Primary Environment is Debian Online
only; Offline VM acceptance is `PL9`'s job per the plan.

## Final Status

DONE

---

## PL6 — Done

Built `installation.py` — the Platform's first operation that actually
changes host/Docker state. Real Docker images (a trivial `busybox`-based
backend, genuinely `docker build`/`docker save`d, not placeholder bytes)
replaced the placeholder `docker-images/backend.tar` in
`valid-release-1.0.0`/`valid-release-1.1.0`, and their `install_offline.sh`
scripts were rewritten to do real work (`docker load` + `docker compose
up`) instead of an echo stub — necessary for this slice to prove anything
real at all. Four more fixtures built for the remaining required tests:
`install-script-fails`, `missing-install-script`, `install-script-timeout`,
`install-with-secret`. All schema-validated and checksum-verified before
use, same discipline as `PL3`.

**Real infrastructure gap found and fixed, not worked around**: the
Platform backend's own Docker image never had the `docker`/`docker
compose` CLI installed (only `docker-py`, used for API calls, not shelling
out). Since §12.6 requires the Platform to invoke Manifest-declared
lifecycle scripts rather than reimplement their behavior, and this
Golden Fixture's install script legitimately needs to run real `docker
load`/`docker compose up` as real shell commands, the backend image
needed the real Docker CLI + Compose plugin installed — added to the
`Dockerfile`, matching the same Docker-outside-of-Docker pattern already
established since PL0, just extended to the client-CLI side. Confirmed
this is real and necessary by installing it and having the manual
end-to-end fixture test go from `pull access denied` (see below) to
working.

**Synchronous/asynchronous boundary implemented for real, not
approximated**: `install_application()` does request validation, release
availability, already-installed, fresh-install-support, configuration
validation, and the port recheck *synchronously* — the caller gets an
immediate rejection with no operation ever created for anything already
known to be wrong (live-verified for the Port Conflict test: zero
`operations` rows afterward). Only once the operation is created and
`RUNNING` does actual execution — prepare, script, verify, commit — run
in a background thread, matching §5.3's long-running-operation model and
the plan's own "202 Accepted → operation_id → RUNNING" wording. Endpoint
returns real HTTP `202`.

**Minimal verification, deliberately not `PL7`'s full verification**:
`_minimal_verify()` does real Docker container inspection (via
`docker-py`, checking the Compose project's expected services are
present and `running`) — it does **not** invoke the Release's own
`verify_deployment.sh`, which stays entirely `PL7`'s job. This matches
the plan's own explicit scope boundary for this slice.

**Real design overlap found and fixed while writing tests**: `PL5`'s
`validate_deployment_inputs` had grown a live port-availability check of
its own (beyond its own required tests — a bonus check added in `PL5`).
That check fired *before* `PL6`'s own dedicated "recheck ports
immediately before execution" step ever ran, duplicating §3.7's own step
separation (general request validation in step 1, a distinct port
recheck in step 3) and producing the wrong error code for the Port
Conflict test. Removed the live check from `validate_deployment_inputs`
(kept the datatype check, which is what its own required tests actually
need) so `PL6`'s dedicated recheck is the one authoritative "is this port
available right now" gate before execution, exactly matching the
architecture's own two-step structure.

**Deployment staging is a deliberate Period A simplification**: the
canonical path (`/opt/rah/apps/<slug>`) is staged inside the Platform
backend container's own filesystem, not a real host-mounted directory.
The Golden Fixtures' Compose files declare no bind mounts, so this
sidesteps a real Docker-outside-of-Docker complication (a bind-mount path
issued from inside a DooD container is resolved by the *host* daemon,
not the calling container's filesystem) without needing to solve it for
Period A. Flagged for a real fixture/richer test once bind-mounted
persistent app data is ever exercised — likely `PL8` (Backup) territory.

**9 new tests added (122/122 total passing)** — `test_installation.py`
(all 8 required scenarios) and `test_installation_api.py` (the same
success path through the real `202` HTTP surface). Two real bugs found
and fixed while writing these tests, both before anything shipped:

1. The `install-with-secret` fixture's `docker-compose.yml` referenced
   `secret-app-backend:1.0.0`, but the reused image archive was actually
   tagged `golden-test-app-backend:1.0.0` — a real `docker load` +
   `docker compose up` cycle correctly failed with `pull access denied`
   the first time this was run, confirming the test infrastructure
   itself catches real fixture mistakes rather than silently passing.
2. The `PL5`/`PL6` port-check overlap described above.

**Every required test also re-proven live against the real running
Docker Compose container**, not just pytest — including confirming the
started container from *outside* the backend container (on the real
Debian host's own Docker Engine), proving the Docker-outside-of-Docker
chain genuinely works end to end, not just from the caller's own
vantage point:

- **Successful Install** — real `POST .../install` on `valid-release-1.0.0`
  returned real `202` + `RUNNING`; polling reached `SUCCEEDED`; `docker ps`
  **on the host** showed the real container running with the real port
  mapping; `GET .../active-deployment` returned the real deployment.
- **Cross-slice proof** — `GET .../actions` immediately after showed
  `INSTALL: false` (`PLT-APPLICATION-002`) and `VERIFY`/`BACKUP: true` —
  `PL4`'s decision logic correctly reflecting a *real* installation for
  the first time, not seeded Registry state.
- **Installation Script Failure** — real `install-script-fails` fixture,
  real `202`, polled to `FAILED` with real `PLT-INSTALL-005` and the real
  preserved exit code (`7`).

### PL6 Testing Record

| Test ID | Test | Environment | Expected | Observed | Status |
|---|---|---|---|---|---|
| PL6-T01 | Successful Install | Debian Online + Docker | 202 → operation_id → RUNNING → containers start → deployment exists | All confirmed, real container on real host Docker Engine | PASS |
| PL6-T02 | Installation Script Failure | Debian Online + Docker | FAILED, no active deployment, exit code preserved, logs available | Real `PLT-INSTALL-005`, exit code `7`, logs present | PASS |
| PL6-T03 | Port Conflict Immediately Before Execution | Debian Online + Docker | Stop before lifecycle script | Real `PLT-CONFIG-004`, zero operations created | PASS |
| PL6-T04 | Missing Script | Debian Online + Docker | Fail safely | Real `PLT-INSTALL-004` | PASS |
| PL6-T05 | Script Timeout | Debian Online + Docker | Fail predictably | Real `PLT-SCRIPT-003` after real subprocess timeout | PASS |
| PL6-T06 | Operation Lock | Debian Online + Docker | Second install/update rejected | Real `PLT-LOCK-001` | PASS |
| PL6-T07 | Secret Handling | Debian Online + Docker | Secret does not leak into events/logs | Confirmed absent from real operation history; present in rendered `.env` (needed by container) | PASS |
| PL6-T08 | Registry Commit Failure | Debian Online + Docker | Reconciliation required, not false success | Real `PLT-INSTALL-007`, `reconciliation_required: true`, container genuinely running but not claimed active | PASS |

## Problems Discovered

### PL6-I01 — Backend image missing the Docker CLI

See "Real infrastructure gap" above. Fixed in the `Dockerfile`, not
worked around.

### PL6-I02 — `install-with-secret` fixture referenced the wrong image tag

See "9 new tests" above. Fixed by correcting the Compose file to the
real loaded tag and recomputing checksums.

### PL6-I03 — `PL5`'s `validate_deployment_inputs` duplicated `PL6`'s port recheck

See "Real design overlap" above. Fixed by removing the live-availability
check from `PL5`'s function, keeping only its own required datatype
check.

## Decisions Made

- **Docker CLI + Compose plugin added to the backend image.** Not
  optional — Manifest-declared lifecycle scripts need a real `docker`
  binary to do real work, and the Platform must invoke them rather than
  reimplement their behavior (§12.6). Installed via Docker's own apt
  repository, matching how a real Debian host would get it.
- **Minimal verification is real Docker inspection, not the Release's
  own verify script.** Keeps the `PL6`/`PL7` boundary the plan itself
  drew: `PL6` proves execution mechanics with "sufficient to distinguish
  obvious success/failure," `PL7` owns real depth.
- **Deployment staging is container-local, not a real host-mounted
  `/opt/rah/apps/`.** A deliberate, flagged Period A simplification — see
  "Deployment staging" above. Real host-persistent deployment directories
  (and the DooD bind-mount-path complication that comes with them) are
  deferred until a slice actually needs them.
- **`validate_deployment_inputs`'s live port check was removed, not
  kept alongside `PL6`'s.** Two independent live-availability checks for
  the same port, at two different times, is confusing and redundant —
  architecture's own step separation (§3.7) already assigns "recheck
  ports immediately before execution" to one specific, late step. `PL5`
  keeps only what its own required tests need (datatype validation).

## Offline Retest

Status: NOT YET REQUIRED — `PL6`'s Primary Environment is Debian Online +
Docker only; Offline VM acceptance is `PL9`'s job per the plan.

## Final Status

DONE

## PL7 — Done

Built `verification.py` — the slice that completes the three-authority
model (§7.1: Release Manifest = expected, Platform Registry = recorded,
Host Inspection = observed). `PL6`'s `_minimal_verify()` (container
running, nothing more) is retired; `installation.py` now calls
`verification.run_verification()` directly, so a fresh install's own
post-install check is the same real check set a standalone `POST
.../verify` call would run — not two divergent implementations.

**Check set implemented against real fixture capabilities, not a fixed
abstract list**: `release_identity`, `container_existence`,
`container_health`, `image_tags`, `selected_port`, `offline_runtime`,
and `persistent_configuration` are mandatory (`NOT_EXECUTED` on any of
these fails the run); `database_connectivity`, `migration_state`,
`backend_health`, and `frontend_reachability` are honestly
`NOT_APPLICABLE` unless a Release's own manifest declares them required
— reporting a fabricated `PASS` for a capability nothing actually probed
would violate §5.24's Result Authority Principle. Two new fixtures built
to make `backend_health` and multi-container drift genuinely testable:
`backend-health-fails` (a real image that never serves HTTP, so the
check produces a real `FAIL`, not a simulated one) and `multi-service-app`
(two real containers under one Compose project, for `PARTIALLY_RUNNING`
reconciliation).

**New schema — migration `0007`**: `verification_runs` +
`verification_checks` (every run preserved independently — §7.25's
Verification History Rule: "a later passing verification shall not erase
an earlier failure" — proven directly by `test_verification_history_
preserves_earlier_failure`, which stops a real container, verifies the
real `FAIL` is retrievable, restarts it, verifies the real `PASS` is a
*different* run, and confirms the original `FAIL` is still there
unmodified) and `reconciliations` (recorded drift findings, §7.27's Host
Drift Rule: "shall not silently modify historical state to make the
mismatch disappear").

**Reconciliation state logic, priority-ordered by design**: `UNKNOWN`
(no active deployment at all) → `UNREACHABLE` (Docker inspection itself
failed) → `DRIFT_DETECTED` (zero of the expected containers running, or
any image-tag/existence mismatch) → `PARTIALLY_RUNNING` (some but not
all expected containers running) → `CONSISTENT`. All five states were
exercised with real Docker state changes, not mocks — including
`UNREACHABLE`, produced by making `_containers_by_service` genuinely
raise rather than simulating the exception at a higher layer, so the
real `try`/`except` path in `reconcile_application_state` is what's
under test.

**A real, pre-existing fixture bug found by this slice's own new check,
not introduced by it**: `install-with-secret`'s manifest still declared
`docker.images[].repository: secret-app-backend` — a leftover from
`PL6-I02` (see PL6's Problems Discovered), where only the *Compose
file's* image reference was corrected to `golden-test-app-backend`, not
the manifest's own `docker.images` declaration the two are supposed to
agree with. `PL6`'s `_minimal_verify()` never compared image tags, so
this went undetected until `PL7`'s real `image_tags` check produced a
genuine `FAIL` the first time `test_secret_does_not_leak_into_events_or_logs`
was re-run under the new verification path. Fixed by correcting the
manifest field and recomputing `release.yaml`'s checksum — the same
"the test infrastructure itself catches real fixture mistakes" pattern
`PL6` established, now caught by a *different* slice's real check,
confirming the checks are actually independent and not rubber-stamping
each other.

**`_resolve_expected_release` bug found and fixed before anything
shipped**: an early draft returned `application_row["active_deployment_id"]`
(a deployment identifier) where the function's contract — inferring
`expected_release_id` for a manual verification call that omitted it
(§4.15: "The Platform may infer `expected_release_id` from the active
deployment when omitted") — requires a real `release_id`. Fixed to look
up the `deployments` row for that `active_deployment_id` and return its
`release_id` instead; `test_resolve_expected_release_infers_release_id_
not_deployment_id` asserts the two identifiers actually differ and that
the resolved value matches the real imported Release, not the deployment.

**New catalog gap flagged, same pattern as `PLT-VERIFY-009`**: no code
in §8.17 fits "no active deployment exists to infer `expected_release_id`
from" (every existing `PLT-VERIFY-*` entry is a check-failure code, and
reusing `ApplicationNotFoundError` would be wrong — the Application
genuinely exists, it just has nothing installed). Added `PLT-VERIFY-010`
(`NoActiveDeploymentError`), flagged for the same later namespace review
as `PLT-VERIFY-009` and the `PLT-STORAGE-004` reuse.

**Deliberate deviation from §6.28's literal `202 Accepted`**: `POST
.../verify` runs synchronously and returns `200`, not `202` +
operation-then-poll. Verification here is a handful of fast Docker/
Registry checks (milliseconds, not a long-running script) — `PL6`'s
async pattern exists because a lifecycle script can run for minutes;
nothing here does. The operation is still created and recorded through
the Operation Framework either way (§2.6), so the *recording* discipline
is unchanged, only the client-facing wait model. Flagged explicitly in
both `verification.py`'s and `app.py`'s docstrings rather than silently
diverging.

**21 new tests added (143/143 total passing)** — `test_verification.py`
(16 tests: all required scenarios plus the `_resolve_expected_release`
regression and error-path coverage) and `test_verification_api.py` (5
tests, the same scenarios through the real HTTP surface). Every test
uses real Docker state — real containers stopped/started/removed/
recreated with mismatched tags or ports via `docker-py`, never mocked —
matching this project's standing "real container, not mocked" standard.

**Every required test also re-proven live against the real running
Docker Compose container** on this Online Debian VM, through a full
real scan → import → install → verify → host-state → reconcile cycle:

- **Healthy Installation** — real `POST .../install` on
  `valid-release-1.0.0` reached `SUCCEEDED`; the install's own internal
  verification (recorded under the `INSTALL` operation, `verification_type:
  POST_INSTALL`) showed `status: PASS`, 7 real `PASS` checks.
- **Verify Deployment** — real `POST .../verify` with no
  `expected_release_id` correctly inferred the real Release (confirmed
  distinct from the deployment id), returned `PASS` with the identical
  check set.
- **Read-Only Host Inspection** — real `GET .../host-state` showed the
  real container `RUNNING`, real image tag match, real published port
  `19100`; calling it twice produced no new operation and no container
  restart.
- **Reconciliation** — real `POST .../reconcile` returned `CONSISTENT`
  with an empty `drift_items`; after manually `docker stop`-ping the real
  container from **outside** the backend container (the real Debian
  host's own Docker Engine, same cross-boundary proof pattern `PL6`
  established), re-reconciling returned real `DRIFT_DETECTED` with a
  real `CONTAINER_NOT_RUNNING` drift item — the Platform detected genuine
  host drift it did not cause and did not silently paper over.
- **Get Verification Result** — real `GET .../verifications/{id}`
  returned the persisted run by id, matching what `POST .../verify` had
  returned synchronously.

All live-test Registry rows and the real installed container/network
were torn down after verification; the shared Debian VM's Platform stack
was confirmed healthy (`READY`, all three checks `PASS`) afterward.

### PL7 Testing Record

| Test ID | Test | Environment | Expected | Observed | Status |
|---|---|---|---|---|---|
| PL7-T01 | Healthy Installation | Debian Online + Docker | Post-install verification PASS, all mandatory checks PASS/NOT_APPLICABLE | Confirmed, real install wired to real `run_verification` | PASS |
| PL7-T02 | Failed Backend Health | Debian Online + Docker | `backend_health` check FAILs against a real non-serving container | Real FAIL, overall status FAIL | PASS |
| PL7-T03 | Missing Container | Debian Online + Docker | `container_existence` FAILs when nothing was ever installed | Real FAIL, missing service correctly named | PASS |
| PL7-T04 | Wrong Image Tag | Debian Online + Docker | `image_tags` FAILs against a real mismatched running container | Real FAIL, correct expected/observed tags reported | PASS |
| PL7-T05 | Changed Port | Debian Online + Docker | `selected_port` FAILs when the observed publish differs from recorded config | Real FAIL, correct expected/observed ports reported | PASS |
| PL7-T06 | Manual Container Stop | Debian Online + Docker | `container_health` FAILs after a real `docker stop` | Real FAIL, correct service named | PASS |
| PL7-T07 | Verification History | Debian Online + Docker | An earlier real FAIL is not erased by a later real PASS | Both runs independently retrievable, distinct ids | PASS |
| PL7-T08 | Reconciliation — all 5 states | Debian Online + Docker | UNKNOWN/CONSISTENT/PARTIALLY_RUNNING/DRIFT_DETECTED/UNREACHABLE all reachable with real state | All 5 confirmed with real Docker state changes | PASS |
| PL7-T09 | Read-Only Host Inspection | Debian Online + Docker | No side effects from repeated calls | Confirmed: no new operations, no container changes | PASS |

## Problems Discovered

### PL7-I01 — `install-with-secret` fixture's manifest still referenced the wrong repository

See "A real, pre-existing fixture bug" above. A leftover from `PL6-I02`
where only the Compose file was corrected, not the manifest's own
`docker.images` declaration. Fixed by correcting `release.yaml` and
recomputing its checksum.

### PL7-I02 — `_resolve_expected_release` returned a deployment id, not a release id

See "`_resolve_expected_release` bug" above. Fixed before shipping;
covered by a dedicated regression test.

## Decisions Made

- **`installation.py` now delegates entirely to `verification.
  run_verification()`.** No parallel "minimal" check survives — the
  install path and the standalone verify path share one real
  implementation, so they can never silently diverge.
- **`persistent_configuration` is honestly `NOT_APPLICABLE` on a fresh
  install, not `PASS`.** Registry commit happens *after* verification in
  the install sequence (§3.7's own ordering — verify before commit), so
  there is genuinely no prior deployment configuration to compare
  against yet. Reporting `PASS` would be a fabricated claim about
  something that was never actually checked.
- **`POST .../verify` runs synchronously (`200`), not `202` +
  operation-then-poll**, despite §6.28's literal example. See "Deliberate
  deviation" above — flagged explicitly rather than silently diverging
  from the architecture text.
- **`PLT-VERIFY-010` (`NoActiveDeploymentError`) added as a new,
  flagged catalog gap**, distinct from `ApplicationNotFoundError` — the
  Application exists, it simply has nothing installed to infer a Release
  from.

## Offline Retest

Status: NOT YET REQUIRED — `PL7`'s Primary Environment is Debian Online +
Docker only; Offline VM acceptance is `PL9`'s job per the plan.

## Final Status

DONE

## PL8a — Done

Built `backup.py` and `update.py` — the second major lifecycle
transition (§3.8/§9.21): `Existing application (active Release) → New
Release, existing configuration/data preserved`. Per this review's own
"One slice too large: PL8" recommendation above, tracked and tested as
`PL8a` (Backup + Update) first, `PL8b` (Recovery) next — the proposal's
own narrative/tests are unchanged, only the status-tracking granularity.

**Real sequencing, matching §9.19/§9.22/§9.26 exactly**: `BACKING_UP`
(real, mandatory-if-declared, shares the parent `UPDATE` operation's own
`operation_id` — architecture "Choose Implementation Mechanisms" doc
§7.5.5's "Shared Operation for Sub-Steps") → `EXECUTING_SCRIPT` (the
target Release's own `entrypoints.update` script, real subprocess, same
primitive as `PL6`'s install) → `MIGRATING` (if declared, real script +
real captured exit code as migration evidence — Period A does not
implement a database-level migration-*state* check, matching `PL7`'s
own already-stated boundary for `database_connectivity`/
`migration_state`) → `VERIFYING` (reuses `PL7`'s `run_verification`
wholesale, `verification_type="POST_UPDATE"`) → `RECORDING_RESULT`
(only reached after a real `PASS` — §9.22 Failed Update Rule: "An
unsuccessful update SHALL NOT overwrite the last known successful
active-deployment record").

**New schema — migration `0008`**: `backups` (every backup's own
storage location, checksum, and artifact-lifecycle status, kept
independent of `operations.status` for the same reason `PL7`'s
`verification_runs` is — the artifact's own lifecycle continues to
change after its creating operation reaches a terminal state). No new
columns added to `deployments` — "was a backup required/completed for
this update" is derivable from the release's own manifest declaration
plus a `backups` row keyed by the update's own `operation_id`, so no
migration was needed on an existing table.

**Configuration preservation is real, not just planned**: `PL5`'s own
`preserved_configuration`/`preserved_secret_keys` helpers (promoted from
private to shared, no behavior change, full suite green before and
after — same pattern as `PL5`'s own promotion of `PL4`'s lookup
helpers) drive real preservation at execution time. Secrets are the
interesting case: §7.16's Secret-State Rule means the Operational
Registry never stores a secret's real plaintext, so a preserved
secret's actual value is read back from the *previous* deployment's
real rendered `compose/.env` (`installation.read_rendered_env`, the new
inverse of `render_configuration`) before that directory is replaced —
never from the Registry, which only ever had `secret_reference`.
Verified live and in `test_configuration_preservation_port_and_secret`:
a real secret survives a real update into the newly rendered `.env`,
while never appearing in operation logs/events.

**A real, necessary amendment to `PL7`'s `verification.py`**: an
update's own `POST_UPDATE` verification runs *before* the Registry
commit (by design — §9.22 requires the commit to wait until after
verification passes), so the Registry still correctly points at the
*source* Release throughout `VERIFYING`. `PL7`'s original
`_check_release_identity`/`_check_persistent_configuration` both assumed
the Registry already agreed with what was being verified (true for
`PL6`'s fresh-install case, where there's no active deployment yet, but
not true here) and would have produced a spurious `FAIL` on every real
update, mandatory-check or not. Fixed by giving both checks an explicit
`verification_type` parameter: `POST_UPDATE` treats the pre-commit
Registry/config state as the expected, correct state (still delegating
to `image_tags`/`container_health`/`selected_port`/etc. to independently
confirm the *host* actually matches the target). Documented here rather
than silently folded into `PL7`'s own section, since the change was
made necessary by `PL8a`'s real requirements, not discovered within
`PL7` itself.

**A real, pre-existing bug found while building the `update-verify-fails`
fixture, not introduced by it**: none — this fixture's mismatched image
repository is deliberate, unlike `PL7-I01`'s accidental one. Worth
noting the distinction explicitly since both fixtures now contain a
manifest/compose mismatch, for different reasons.

**Which Release's manifest declares `backup_before_update`/`migration`**:
the *target* (the Release being updated to), matching `PL5`'s own
`prepare_update`, which already reads `database.backup_before_update`
from `target_release_row` — a Decision this slice had to make explicit
and follow consistently, not something the architecture text
disambiguates on its own.

**Fixtures**: `valid-release-1.1.0` amended (not replaced) to add real
update/backup/migration support — `supported_operations.update: true`,
`transition.accepted_installed_versions: [1.0.0]`,
`entrypoints.update`/`backup`/`restore`, `database.required: true` with
a real `backup_before_update`/`migration`/`recovery` declaration, and
four new real scripts (`update_offline.sh`, `backup_database.sh`,
`migrate.sh`, `restore_database.sh`). Verified this amendment is safe:
grepped every existing test referencing `valid-release-1.1.0` and
confirmed none assert on its manifest content (only import/discovery
mechanics), and no existing test ever installs `1.1.0` directly. Five
new fixtures built for the remaining required tests, all schema-valid
and checksum-verified: `update-backup-fails`, `update-script-fails`,
`update-migration-fails`, `update-verify-fails` (each a target-only
release for `golden-test-app`, reusing real `valid-release-1.0.0` as the
common update source), and `update-secret-app` (a target for the
existing `install-with-secret` fixture, kept fully separate from the
shared `1.0.0`/`1.1.0` pair specifically to avoid any risk to the ~140
existing tests that already depend on them — see Decisions Made).

**Two real bugs found and fixed while writing tests, before anything
shipped**:

1. `_validate_update_configuration`'s first draft re-ran
   `deployment_planning.validate_deployment_inputs` against a
   preserved-value placeholder dict — but `validate_deployment_inputs`
   does its own required-field presence check internally and returns
   immediately on the first missing field, before any
   preservation-aware logic could run, so every update with a preserved
   required field failed with a false "missing required field."
   Diagnosed with a standalone reproduction script against a real
   Postgres container (not guessed from reading the code), fixed by
   injecting a type-correct *placeholder* value (e.g. `1` for a `port`
   field) for each preserved key before calling the existing validator
   — reusing its real type-checking wholesale rather than
   reimplementing presence-checking separately.
2. Two tests (`test_backup_unsupported_when_active_release_declares_no_
   backup_entrypoint`, originally) wrongly used `pytest.raises` against
   `create_backup()` directly — `create_backup` is async (§6.31: `202
   Accepted`) exactly like `PL6`'s install, so `BackupUnsupportedError`
   only ever surfaces via the operation's terminal `FAILED` state, never
   as a raised Python exception from the call site. A test design bug,
   fixed in the test, not the code.

**14 new tests added (157/157 total passing)** — `test_backup.py` (4),
`test_update.py` (8, covering all 7 required scenarios plus an explicit
Registry-commit-failure regression test mirroring `PL6`'s own), and
`test_update_api.py` (2, the same core flows through the real HTTP
surface). Every test uses real Docker state and real script execution —
no mocked backup/update/migration scripts anywhere.

**Every required test also re-proven live against the real running
Docker Compose container** on this Online Debian VM, through a full
real scan → import → install → update cycle:

- **Successful Update** — real `POST .../update` on a real installed
  `1.0.0` targeting real `1.1.0` reached `SUCCEEDED`; real operation
  events showed the full real sequence (`BACKING_UP` → `BACKUP_COMPLETED`
  → `EXECUTING_SCRIPT` → `MIGRATING` → `MIGRATION_COMPLETED` →
  `VERIFYING` → `VERIFICATION_COMPLETED` → `RECORDING_RESULT`); the real
  active deployment now pointed at `1.1.0`; a real `VERIFIED` backup
  record existed with a real SHA-256 checksum and a real artifact file
  under `/opt/rah/backups/golden-test-app/...`; `docker inspect` **on
  the host** confirmed the real container now running
  `golden-test-app-backend:1.1.0`.
- **Cross-slice proof, PL4** — the real `golden-test-app` application's
  action list and release history correctly reflected the real
  transition, matching what `PL4`'s already-tested decision logic
  predicts for a `PREVIOUSLY_DEPLOYED` source Release.
- **Correctly-rejected transition** — attempting to further update the
  now-`1.1.0`-active application with a fixture whose
  `accepted_installed_versions` only lists `1.0.0` was real-rejected
  with `PLT-UPDATE-001`/`PLT-TRANSITION-003` — confirmed this is
  `PL4`'s real decision engine working as intended, not a bug, before
  moving on.

All live-test Registry rows, the real installed container, and its
network were torn down after verification; the shared Debian VM's
Platform stack was confirmed healthy (`READY`, all three checks `PASS`)
afterward.

### PL8a Testing Record

| Test ID | Test | Environment | Expected | Observed | Status |
|---|---|---|---|---|---|
| PL8a-T01 | Successful Backup | Debian Online + Docker | Real artifact created and registered, `VERIFIED` | Confirmed, real SHA-256 checksum, real file on disk | PASS |
| PL8a-T02 | Backup Failure | Debian Online + Docker | Update never begins (no update script execution) | Real `PLT-UPDATE-002`, no `backups` row, no update-script log line | PASS |
| PL8a-T03 | Successful Update | Debian Online + Docker | `1.0.0 active → backup → update → verify 1.1.0 → 1.1.0 active`, `1.0.0` remains in history | All confirmed, real container on real host Docker Engine, `PL4` reports `PREVIOUSLY_DEPLOYED` for `1.0.0` | PASS |
| PL8a-T04 | Configuration Preservation | Debian Online + Docker | Port and secret remain correct after update, secret never logged | Real preserved port/secret in the new `.env`, absent from real operation history | PASS |
| PL8a-T05 | Update Script Failure | Debian Online + Docker | `1.1.x` does not become active, `1.0.0` remains active | Real `PLT-UPDATE-005`, exit code `6` preserved, backup genuinely succeeded first | PASS |
| PL8a-T06 | Migration Failure | Debian Online + Docker | Update fails with precise evidence | Real `PLT-DATABASE-002`, exit code `7`; real host drift confirmed via `PL7`'s own `reconcile_application_state` | PASS |
| PL8a-T07 | Verification Failure After Update Script Success | Debian Online + Docker | Target remains non-active, recovery requirement exposed | Real `PLT-UPDATE-007`; real container genuinely running the new image despite Registry correctly withholding activation | PASS |

## Problems Discovered

### PL8a-I01 — `_validate_update_configuration` re-validated preserved values with the wrong types

See "Two real bugs" above. Fixed by injecting type-correct placeholders
before calling the existing, reused validator.

### PL8a-I02 — Two tests wrongly expected a raised exception from an async entry point

See "Two real bugs" above. `create_backup`/`update_application` are
both async (`202`) like `PL6`'s `install_application` — failures surface
through the operation's terminal state, not a raised Python exception at
the call site. Fixed in the tests.

## Decisions Made

- **Tracked and tested as `PL8a`/`PL8b`, per this review's own earlier
  recommendation.** No redesign — the proposal's narrative and required
  tests are unchanged, only the status-tracking granularity, exactly as
  recommended above before `PL0` began.
- **`backup_before_update`/`migration` are declared on the *target*
  Release, not the source.** Matches `PL5`'s already-shipped
  `prepare_update`, which reads exactly these fields from
  `target_release_row`. Not dictated by the architecture text; a
  Decision made once and applied consistently.
- **A failed or caller-requested backup always stops the update**,
  whether the backup was mandatory-by-manifest or only requested by the
  caller via `create_backup: true`. §9.19 only mandates this for the
  mandatory case; extended uniformly here since silently proceeding
  without a backup the caller explicitly asked for would be worse than
  failing safe.
- **No live port recheck for a *preserved* port during update** — only
  for a port the caller is actively *changing* via
  `configuration_overrides`. The preserved/current port is expected to
  be in use by the very deployment the update is about to replace;
  checking it live before that replacement would always spuriously
  fail. `PL6`'s own dedicated pre-execution port recheck remains the
  sole authority for fresh installs, where this ambiguity doesn't exist.
- **`update-secret-app` was built as a fully separate fixture** (paired
  with the existing `install-with-secret`) rather than adding a secret
  input to the shared `valid-release-1.0.0`/`1.1.0` pair. Zero regression
  risk to the ~140 existing tests already depending on that pair,
  matching this project's own established precedent (`PL6`'s
  `install-with-secret`, `PL7`'s `backend-health-fails`/
  `multi-service-app` — dedicated fixtures per concern, not shared-fixture
  amendments beyond what's already proven safe).
- **`Backup Restore`, `Recovery`, and `Wrong Application Backup`
  deferred to `PL8b`.** They structurally belong to the restore/recover
  endpoints (`POST /backups/{id}/restore`, `POST /applications/{id}
  /recover`), neither of which `PL8a` implements — matching the
  `PL8a`/`PL8b` split's own stated boundary ("Recovery only activates on
  a controlled failure path... testable independently").

## Offline Retest

Status: NOT YET REQUIRED — `PL8a`'s Primary Environment is Debian Online
+ Docker only; Offline VM acceptance is `PL9`'s job per the plan.

## Final Status

DONE

## PL8b — Done

Built `recovery.py` — the second of two tracked `PL8` sub-slices,
implementing §7.24 (Recovery History Rule) and the recovery-facing half
of §9.22. Also gave `PL4`'s `_evaluate_recover()` (previously a
correctly-reasoned but permanent stub, per its own documented "no
failure-tracking exists until `PL8`") real logic for the first time.

**A deliberately simple, real design**: recovery in Period A never
changes *which* Release is active — it only repairs the *host* to match
what the Registry already, correctly, still claims (§9.22 already
guarantees a failed `INSTALL`/`UPDATE` never falsely overwrote the
active-deployment record, so recovery's job is exactly the mirror image:
fix the host, not the Registry). This eliminated an entire category of
complexity a "restore into the backup's original context" design would
have needed (resolving a possibly-stale historical deployment, deciding
whether to re-commit a Registry pointer, handling edge cases where that
pointer is missing) — `_perform_restore` always targets the *currently
active* deployment, using its own release's real `entrypoints.restore`/
`database.recovery.entrypoint` script. `recover_application` never
calls `installation.commit_deployment` at all.

**`RECOVER` availability is now real, not a stub**: `application_query
._get_last_lifecycle_operation_row` finds the application's most recent
`INSTALL`/`UPDATE` operation (deliberately excluding `VERIFY`/`BACKUP`/
`RECOVER` itself — recovering from a failed verification-only or
backup-only attempt isn't a "restore previous state" scenario the way a
failed install/update is); `RECOVER` is allowed exactly when that
operation exists and is `FAILED`. Live-verified as a genuine before/after
state change, not just a passing assertion: `GET .../actions` reported
`RECOVER: false` before any failure existed, then real `true` immediately
after a real `PLT-UPDATE-007` failure — the same live cross-slice proof
pattern `PL6` established for `PL4`'s `INSTALL` action.

**A real script gap found and fixed before it could hide a bug**: the
first draft of `valid-release-1.0.0`'s new `restore_database.sh` only
copied the backup `.env` file back — sufficient for the "nothing was
actually broken" standalone-restore case, but silently insufficient for
the real Recovery scenario (host running the *target*'s image after
`PLT-UPDATE-007`), where the actual drift is the *container*, not just
its config. Caught by writing the real test first and watching
`reconcile_application_state` still report `DRIFT_DETECTED` after a
"successful" recovery. Fixed by making the script do a real, complete
repair: `docker load` the release's own real image archive, then
`docker compose up` with `--env-file` pointed explicitly at the *active*
deployment's real `.env` (the release's own compose file otherwise
resolves its project directory — and therefore its default `.env`
lookup — to the immutable Release Package's own `compose/` directory,
not the live staged deployment, since this script runs directly from
release storage the same way `PL8a`'s `backup_database.sh` does).

**Fixtures**: `valid-release-1.0.0` amended with one additive field
(`deployment.entrypoints.restore: restore_database.sh`) and one new real
script — verified safe the same way `PL8a` verified its own
`valid-release-1.1.0` amendment: nothing existing reads
`entrypoints.restore`, and `database.required`/`configuration.inputs`
(the fields other tests actually depend on) are untouched. No other new
fixtures were needed — `update-verify-fails` (`PL8a`) already produces
the real failed-update-with-host-drift precondition every Recovery test
needs, and the existing `valid-release-1.1.0`/`install-with-secret`
pairing was enough for the standalone-restore and wrong-application
scenarios.

**6 new tests added (163/163 total passing)** — `test_recovery.py` (5:
Recovery after a real failed update, standalone Backup Restore, Wrong
Application Backup, and two error-path regressions) and
`test_recovery_api.py` (1, the core recovery flow through the real HTTP
surface). Every test uses real Docker state and real script execution —
the core Recovery test alone exercises a real failed update, real drift
detection, a real `RECOVER`-availability flip, a real restore script,
real re-verification, and real drift resolution, all in one continuous
sequence against genuine Docker state.

**Every required test also re-proven live against the real running
Docker Compose container** on this Online Debian VM:

- **Recovery** — a real install of `1.0.0`, a real failed update to
  `update-verify-fails` (`PLT-UPDATE-007`), confirmed real drift
  (`reconcile` → `DRIFT_DETECTED`, real container running
  `golden-test-app-backend:1.1.0`) and confirmed `RECOVER` freshly
  `true` where it had been `false` moments before; a real `POST
  .../recover` reached `SUCCEEDED` with real operation events
  (`RESTORE_COMPLETED` → `VERIFICATION_COMPLETED` →
  `RECOVERY_COMPLETED`); afterward, `reconcile` genuinely reported
  `CONSISTENT` again, `docker inspect` **on the host** confirmed the
  real container back on `golden-test-app-backend:1.0.0`, the backup
  record showed real `RESTORED` status, the active Release never
  changed, and the original failed `UPDATE` operation still shows
  `FAILED` in history — §7.24's "failure and correction both visible"
  requirement, proven with real, distinct operation records rather than
  asserted from the design alone.

All live-test Registry rows, the real container, and its network were
torn down after verification; the shared Debian VM's Platform stack was
confirmed healthy (`READY`) afterward.

### PL8b Testing Record

| Test ID | Test | Environment | Expected | Observed | Status |
|---|---|---|---|---|---|
| PL8b-T01 | Recovery | Debian Online + Docker | Separate recovery operation generated; failed update remains failed in history | Real distinct operation IDs; failed `UPDATE` unchanged; real host repaired to `CONSISTENT` | PASS |
| PL8b-T02 | Backup Restore | Debian Online + Docker | Restore through approved script | Real `SUCCEEDED`, backup marked `RESTORED` | PASS |
| PL8b-T03 | Wrong Application Backup | Debian Online + Docker | Rejected | Real `PLT-BACKUP-007` | PASS |

## Problems Discovered

### PL8b-I01 — `restore_database.sh`'s first draft only restored configuration, not the real container

See "A real script gap found" above. Caught by a real test assertion
(`reconcile_application_state` still `DRIFT_DETECTED` after a
"successful" recovery), not by inspection. Fixed with a real, complete
restore script.

## Decisions Made

- **Recovery never changes which Release is active — only the host.**
  §9.22 already guarantees the Registry's active-deployment record
  survived a failed `INSTALL`/`UPDATE` untouched; recovery's job is the
  mirror repair. This is a real scope decision, not an oversight: a
  design that also handled "the Registry pointer itself is wrong" would
  need to solve reconciliation-with-recommit, deliberately deferred as
  out of Period A's real needs (no test scenario in the plan requires
  it, and `PL7`'s `reconcile_application_state` already exists as the
  read-only detection half of that larger problem, un-automated by
  design per its own Decisions Made).
- **`RECOVER` availability is driven only by the most recent `INSTALL`/
  `UPDATE` operation**, not `VERIFY`/`BACKUP`/`RECOVER` itself. A failed
  verification-only or backup-only attempt didn't change host state the
  way a failed lifecycle operation can, so it isn't a "previous state to
  restore" scenario.
- **`restore_backup` and `recover_application` share one real restore
  implementation** (`_perform_restore`), matching this Platform's
  standing pattern (`PL7`'s `verify_deployment` wrapping
  `run_verification`, `PL8a`'s install/update sharing `commit_deployment`)
  — the broader "recover from a failure" flow and the narrower "restore
  this specific backup" flow can never silently diverge.

## Offline Retest

Status: NOT YET REQUIRED — `PL8b`'s Primary Environment is Debian Online
+ Docker only; Offline VM acceptance is `PL9`'s job per the plan.

## Final Status

DONE
